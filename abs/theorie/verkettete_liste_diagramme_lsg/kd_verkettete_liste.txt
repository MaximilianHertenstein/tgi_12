@startuml
skinparam classAttributeIconSize 0
hide circle
!theme plain
top to bottom direction


class Node {
  - content : String
  + Node(content : String, nextNode : Node)
  + content() : String
  + nextNode() : Node
}

class LinkedList {

  + LinkedList(first : Node)
  + first() : Node

}

Node --> Node : nextNode
LinkedList --> Node : first

@enduml
